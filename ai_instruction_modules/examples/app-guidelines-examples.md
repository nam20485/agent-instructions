# Application Guidelines Examples

## Example 1: global.json Configuration

This example shows how to configure `global.json` to pin the .NET SDK version.

```json
{
  "sdk": {
    "version": "9.0.102",
    "rollForward": "latestFeature"
  }
}
```
